import React from 'react';
import styles from "./inCartButton.module.scss"

const Index = (): JSX.Element => {
    return (
        <button className={styles.container}>
            В корзине
        </button>
    );
};

export default Index;
